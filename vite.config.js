import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/organizap/', // IMPORTANTE: debe coincidir con el nombre de tu repositorio
})